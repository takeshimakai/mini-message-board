extends layout

block content
  h1= title
  p Welcome to #{title}
  a(href="/new")= 'New Message' 
  each val in messages
    div(class="container")
      p= val.text
      p= val.user
      p= val.added